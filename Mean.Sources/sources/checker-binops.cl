fun Check(x: int, y: int)
{
    x * y;
    x / y;
    x % y;

    x + y;
    x - y;

    x == y;
    x != y;
    x < y;
    x > y;
    x <= y;
    x >= y;

    x & y;
    x | y;
    x ^ y;
}

fun MAIN()
{
    Check(33, 34);
}

